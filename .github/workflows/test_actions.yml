name: Asimple Docker action

on:
  issues:
    types: [opened, edited, labeled]

jobs:
  user-and-time:
    runs-on: ubuntu-latest
    name: Running a simple Bash script
    steps:
      - name: A simple bash script
        id: BASH
        uses:
          agbleze/a-docker-action@master